# TestWireMockCallback  
Example of how to use WireMock callbacks to simulate an API that has eventual consistency, for example the Azure AD Graph API.

To execute tests run `docker-compose up --build`
